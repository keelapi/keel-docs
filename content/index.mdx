---
title: Home
description: Keel is a permit-driven governance and execution control layer in front of provider APIs.
---

import Link from 'next/link'

# Keel Docs

Keel is a permit-driven governance and execution control layer in front of provider APIs. It gives teams a single place to enforce decisioning, routing, budget controls, and auditability before AI traffic reaches a model provider.

<div className="keel-home-nav">
  <Link className="keel-home-nav__item" href="/overview">
    <span className="keel-home-nav__title">Overview</span>
    <div className="keel-home-nav__description">
      Use the docs map to choose the right starting point across product, architecture,
      and security.
    </div>
  </Link>
  <Link className="keel-home-nav__item" href="/what-is-keel">
    <span className="keel-home-nav__title">What is Keel</span>
    <div className="keel-home-nav__description">
      Understand the product boundary, active public surfaces, and the current scope
      of the platform.
    </div>
  </Link>
  <Link className="keel-home-nav__item" href="/quickstart">
    <span className="keel-home-nav__title">Quickstart</span>
    <div className="keel-home-nav__description">
      Bring up the backend locally, mint a project key, and send one governed request
      through Keel.
    </div>
  </Link>
  <Link className="keel-home-nav__item" href="/architecture">
    <span className="keel-home-nav__title">Architecture</span>
    <div className="keel-home-nav__description">
      Review the runtime modes, shared pipeline, beta doctrine, and persisted
      lifecycle model behind the product.
    </div>
  </Link>
  <Link className="keel-home-nav__item" href="/security">
    <span className="keel-home-nav__title">Security</span>
    <div className="keel-home-nav__description">
      Read the current security posture, isolation model, request-freshness controls,
      and prompt-firewall limits.
    </div>
  </Link>
</div>

## What Keel covers

- permit decisions before provider dispatch
- provider-neutral and provider-native execution paths
- routing, fallback, and budget controls
- prompt inspection on supported execution routes
- persisted audit records, timelines, and compliance exports

## Recommended path

1. Read [Overview](/overview) to choose the right path through the docs.
2. Read [What is Keel](/what-is-keel) to understand the product boundary and active public surfaces.
3. Run [Quickstart](/quickstart) to get a governed request through the stack.
4. Use [Architecture](/architecture) and [Execution Spine](/execution-spine) to understand the runtime.
5. Review [Threat Model](/threat-model) and [Security](/security) before production rollout.

## API reference

The API reference section now includes authored docs for the first public developer-facing routes. Start with [API Reference](/api-reference), then use [Permits](/permits), [Executions](/executions), [Execute](/execute), [Proxy Execution](/proxy-execution), [Idempotency](/idempotency), and [Errors](/errors).
