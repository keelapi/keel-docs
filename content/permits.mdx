---
title: Permits
description: Canonical permit contract for decision-first Keel integrations.
---

# Permits

Use this API when you want Keel to evaluate a request, persist the decision, and let your application decide what to do next.

> Permit creation requires `project_id` in the body.

## When to use permits vs execution routes

- **You control the provider call** — use `POST /v1/permits`. Keel evaluates policy and returns a decision. Your app calls the provider and reports usage back.
- **You want Keel to call the provider** with a portable, provider-neutral request — use [`POST /v1/executions`](/executions).
- **You have provider-shaped payloads** but want normalized output — use [`POST /v1/execute`](/execute).
- **You want provider-native input and output** — use [`POST /v1/proxy/*`](/proxy-execution).

All execution routes evaluate a permit internally. The difference is whether *you* or *Keel* makes the provider call.

## Route summary

| Route | Status | Purpose |
| --- | --- | --- |
| `POST /v1/permits` | Canonical public | Evaluate a permit request and persist a permit row. |
| `GET /v1/permits` | Official public | List permit audit rows. |
| `GET /v1/permits/{permit_id}` | Official public | Fetch one permit audit row. |
| `GET /v1/permits/export` | Official public | Export a project audit bundle. |
| `POST /v1/permits/{permit_id}/usage` | Official public | Report final usage for permit-first callers. |
| `POST /v1/projects/{project_id}/permits` | Compatibility | Older path-bound permit create flow. |

## Authentication

```text
Authorization: Bearer <project_api_key>
```

`X-API-Key` remains accepted for compatibility, but Bearer auth is the documented contract.

## HTTP status codes

| Code | Meaning |
| --- | --- |
| `200` | Permit created. Both `allow` and `deny` decisions return 200 — branch on `decision`, not status code. |
| `400` | Malformed payload or validation failure (invalid subject, action, or resource structure). |
| `401` | Missing or invalid API key. |
| `403` | API key scope insufficient, or `project_id` in the body does not match the key's project. |
| `409` | Idempotency key reused with a different request payload. |
| `500` | Unhandled server error. |

## Idempotency

`POST /v1/permits` uses the body field `idempotency_key` (not an HTTP header).

> Execution routes (`/v1/executions`, `/v1/execute`, `/v1/proxy/*`) use the **HTTP header** `Idempotency-Key` instead. Do not mix the two — body keys are ignored on execution routes, and headers are ignored on permits.

- same key plus the same semantic permit payload replays the prior permit
- same key plus a different semantic payload returns `409 conflict`
- if the client omits the field, Keel currently generates and persists a new `idem_<uuid>` value

That default makes onboarding ergonomic, but retries without a client-supplied key do not get a stable client-controlled replay contract.

## Request shape

> The bearer token is project-scoped, but `POST /v1/permits` still requires `project_id` in the request body to explicitly bind the permit row to a project.

Start here. Fields marked *optional* can be omitted for a first call — add them when your policy needs estimates, routing hints, or request context.

```jsonc
{
  "project_id": "<project_uuid>",
  "idempotency_key": "permit-demo-001",
  "subject": {
    "type": "user",
    "id": "usr_123"
  },
  "action": {
    "name": "ai.generate.summary"
  },
  "resource": {
    "type": "request",
    "id": "req_123",
    "attributes": {
      "provider": "openai",
      "model": "gpt-4o-mini",
      "operation": "generate.text",
      "modality": "text",                          // optional
      "execution_mode": "sync",                    // optional
      "estimated_input_tokens": 200,               // optional
      "estimated_output_tokens": 250,              // optional
      "max_output_tokens_requested": 300,          // optional
      "routing": {                                 // optional
        "priority": "cheap",
        "task_type": "summarization",
        "latency_target_ms": 1500,
        "allow_cross_provider_fallback": false,
        "fallback_chain": []
      }
    }
  },
  "context": {                                     // optional, permit-only
    "timestamp": "2026-03-09T00:00:00Z",
    "ip": "127.0.0.1",
    "user_agent": "curl"
  }
}
```

## Top-level fields

| Field | Required? | Current meaning |
| --- | --- | --- |
| `project_id` | Yes | Project that owns the permit request and resulting permit row. You can find your `project_id` in the Keel dashboard under Project Settings. |
| `idempotency_key` | No | Client-supplied replay key for safe retries. |
| `subject` | Yes | Actor the decision applies to. |
| `action` | Yes | Requested governed action. |
| `resource` | Yes | Request-shaped resource and execution metadata under evaluation. See `resource` fields below. |
| `context` | No | Optional request context such as timestamp, IP, or user agent. This public top-level field is permit-only. |

## `resource` fields

| Field | Required? | Current meaning |
| --- | --- | --- |
| `resource.type` | Yes | Caller-supplied label for the resource kind. Not validated against an enum — any non-empty string is accepted. Common values: `"request"`, `"ai.chat.completion"`. |
| `resource.id` | Yes | Caller-generated identifier for this resource instance. Not validated beyond being a non-empty string — no UUID or format requirement. Use this to correlate the permit with your internal request tracking. |
| `resource.attributes` | Yes | Execution metadata. See fields below. |

## `resource.attributes` fields

| Field | Required? | Current meaning |
| --- | --- | --- |
| `provider`, `model` | Yes | Requested provider and model identity. |
| `operation` | Yes | Canonical governed operation such as `generate.text`, `generate.image`, or `transcribe.audio`. |
| `modality` | No | Execution context label used by routing, capability metadata, and audit records. |
| `execution_mode` | No | `sync`, `async`, or `realtime`. |
| `estimated_input_tokens`, `estimated_output_tokens` | No | Current permit-time estimate fields. |
| `max_output_tokens_requested` | No | Requested upper bound for generated output. |
| `inputs` | No | Declared inputs or assets. |
| `asset_summary` | No | Optional asset metadata summary. |
| `routing` | No | Optional routing hints and fallback metadata. On `POST /v1/permits` these are recorded, not executed. |
| `callback_url` | No | Optional callback target relevant to async execution. |

## `routing` and `context` on permits

- On permits, `resource.attributes.routing` is governance intent and recorded decision context. It can influence policy evaluation and audit records, but `POST /v1/permits` does not dispatch provider work or execute fallback.
- `context` exists on permits as a public top-level request field. Execution routes do not expose a public top-level `context` field.

Compatibility note:

- canonical docs prefer `resource.attributes.*`
- top-level compatibility aliases for `operation`, `execution_mode`, `callback_url`, and token fields still exist for older clients

## What `POST /v1/permits` does not do

- it does not execute provider work
- it does not run Prompt Firewall on raw prompt bodies
- it does not apply provider-bound routing fitness or fallback attempts
- it does not replace `POST /v1/executions`, `POST /v1/execute`, or the provider-specific proxy routes

## API example

### JavaScript

```js
const response = await fetch('https://api.keelapi.com/v1/permits', {
  method: 'POST',
  headers: {
    Authorization: 'Bearer keel_sk_your_key_here',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    project_id: '<project_uuid>',
    idempotency_key: 'permit-demo-001',
    subject: {
      type: 'user',
      id: 'usr_123'
    },
    action: {
      name: 'ai.generate.summary'
    },
    resource: {
      type: 'request',
      id: 'req_123',
      attributes: {
        provider: 'openai',
        model: 'gpt-4o-mini',
        operation: 'generate.text',
        modality: 'text',
        execution_mode: 'sync',
        estimated_input_tokens: 200,
        estimated_output_tokens: 250,
        max_output_tokens_requested: 300
      }
    }
  })
})

const data = await response.json()
console.log(data)
```

### Python

```python
import requests

response = requests.post(
    "https://api.keelapi.com/v1/permits",
    headers={
        "Authorization": "Bearer keel_sk_your_key_here",
        "Content-Type": "application/json",
    },
    json={
        "project_id": "<project_uuid>",
        "idempotency_key": "permit-demo-001",
        "subject": {
            "type": "user",
            "id": "usr_123",
        },
        "action": {
            "name": "ai.generate.summary",
        },
        "resource": {
            "type": "request",
            "id": "req_123",
            "attributes": {
                "provider": "openai",
                "model": "gpt-4o-mini",
                "operation": "generate.text",
                "modality": "text",
                "execution_mode": "sync",
                "estimated_input_tokens": 200,
                "estimated_output_tokens": 250,
                "max_output_tokens_requested": 300,
            },
        },
    },
)

print(response.json())
```

## Response examples

> **Both allow and deny return HTTP 200.** Permit denials are decisions, not errors. Branch on `decision`, not on HTTP status code. This differs from execution routes, which return HTTP 403 for denials.

### Allow response (HTTP 200)

```json
{
  "id": "permit_01jyf4m3n8q2r6t9v1w5x7y0z",
  "decision": "allow",
  "reason": "request_within_model_and_budget_policy",
  "actions": [
    {
      "type": "allow",
      "message": "Allowed by base policy."
    },
    {
      "type": "report_usage",
      "message": "Report actual token counts after execution."
    }
  ],
  "constraints": {
    "max_output_tokens": 300,
    "allowed_modalities": ["text"],
    "execution_mode": "sync"
  },
  "budgets": {
    "tokens": {
      "requested": 450,
      "approved": 450,
      "remaining_project_daily": 184550
    },
    "usd": {
      "approved": 0.0021,
      "remaining_project_daily": 42.73
    }
  },
  "decision_details": {
    "outcome": "policy_allow",
    "matched_policies": [
      "pol_text_generation_default",
      "pol_project_daily_budget"
    ],
    "rule_id": "rule_allow_openai_summary_sync"
  },
  "routing": {
    "requested": {
      "provider": "openai",
      "model": "gpt-4o-mini"
    },
    "selected": {
      "provider": "openai",
      "model": "gpt-4o-mini"
    },
    "reason_code": "requested_route_accepted"
  },
  "metadata": {
    "policy_id": "pol_text_generation_default",
    "policy_version": "2026-03-15",
    "evaluated_at": "2026-03-26T18:42:11Z"
  }
}
```

### Deny response (HTTP 200)

```json
{
  "id": "permit_01jyf4p6c3m7n9q2t5v8w1x4y",
  "decision": "deny",
  "reason": "model_not_permitted_for_project",
  "actions": [
    {
      "type": "deny",
      "message": "Rate limited."
    }
  ],
  "metadata": {
    "policy_id": "pol_model_allowlist",
    "policy_version": "2026-03-15",
    "evaluated_at": "2026-03-26T18:43:02Z"
  },
  "decision_details": {
    "outcome": "policy_deny",
    "matched_policies": [
      "pol_model_allowlist"
    ],
    "rule_id": "rule_block_unapproved_models",
    "deny_code": "model_not_allowed"
  }
}
```

## `actions` array

The `actions` array tells your application what to do next. Iterate it — do not ignore it.

| Action type | Meaning |
| --- | --- |
| `allow` | Permit approved. Proceed to provider execution. |
| `deny` | Request rejected. The action's `message` field gives the reason. |
| `challenge` | External attestation required before execution (e.g., user confirmation, MFA). |
| `review` | Policy review required before the request can proceed. |
| `report_usage` | After execution, report actual token counts back via `POST /v1/permits/{permit_id}/usage`. |

Each action object has `type` (string) and `message` (string). Both fields are always present — `message` is non-nullable on all action types, not just denials. A single response can contain multiple actions — for example, `allow` + `report_usage` means "proceed, then report back."

## Report usage

After an allowed permit completes, report final usage on the permit row:

`POST /v1/permits/{permit_id}/usage`

```bash
curl -sS -X POST https://api.keelapi.com/v1/permits/permit_01jyf4m3n8q2r6t9v1w5x7y0z/usage \
  -H "Authorization: Bearer keel_sk_your_key_here" \
  -H "Content-Type: application/json" \
  -d '{
    "usage": {
      "input_tokens": 182,
      "output_tokens": 247
    }
  }'
```

```json
{
  "ok": true,
  "permit_id": "permit_01jyf4m3n8q2r6t9v1w5x7y0z",
  "usage_recorded": true
}
```

## Response shape

Permit responses include:

- `decision`, `reason`, and `actions`
- `constraints` and `budgets` when permit enforcement produced them
- `decision_details` for explicit deny or challenge metadata
- `routing` with requested and selected provider-model metadata plus reason codes
- `metadata.policy_id`, `metadata.policy_version`, and `metadata.evaluated_at`

## End-to-end permit flow

Use this when your app controls the provider call and wants decision-first governance.

### Step 1 — Create the permit

```bash
curl -sS -X POST https://api.keelapi.com/v1/permits \
  -H "Authorization: Bearer keel_sk_your_key_here" \
  -H "Content-Type: application/json" \
  -d '{
    "project_id": "<project_uuid>",
    "idempotency_key": "flow-001",
    "subject": {"type": "user", "id": "usr_123"},
    "action": {"name": "ai.generate.summary"},
    "resource": {
      "type": "request",
      "id": "req_flow_001",
      "attributes": {
        "provider": "openai",
        "model": "gpt-4o-mini",
        "operation": "generate.text",
        "estimated_input_tokens": 120,
        "estimated_output_tokens": 80,
        "max_output_tokens_requested": 120
      }
    }
  }'
```

Allow response (HTTP 200):

```json
{
  "id": "permit_01jyf4m3n8q2r6t9v1w5x7y0z",
  "decision": "allow",
  "reason": "request_within_model_and_budget_policy",
  "actions": [
    {"type": "allow", "message": "Allowed by base policy."},
    {"type": "report_usage", "message": "Report actual token counts after execution."}
  ]
}
```

### Step 2 — Check the decision

```ts
const permit = await response.json()

if (permit.decision !== 'allow') {
  const denyCode = permit.decision_details?.deny_code ?? permit.reason
  throw new Error(`blocked: ${denyCode}`)
}

// Check if usage reporting is expected
const shouldReportUsage = permit.actions?.some((a: { type: string }) => a.type === 'report_usage')
const permitId = permit.id
```

If `decision` is `"deny"`, do not call the provider. Read `decision_details.deny_code` for the specific reason. If `actions` contains `"challenge"` or `"review"`, handle those flows before proceeding.

### Step 3 — Call the provider directly

Your app uses its own provider credential for this step. Keel does not proxy the call in permit-only mode.

```ts
const openaiResponse = await fetch('https://api.openai.com/v1/chat/completions', {
  method: 'POST',
  headers: {
    Authorization: `Bearer ${process.env.OPENAI_API_KEY!}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'gpt-4o-mini',
    messages: [{ role: 'user', content: 'Summarize this document in one sentence.' }],
    max_tokens: 120
  })
})

const completion = await openaiResponse.json()
const inputTokens = completion.usage?.prompt_tokens ?? 0
const outputTokens = completion.usage?.completion_tokens ?? 0
```

### Step 4 — Report usage

Report final token counts back to Keel so the permit row reflects actual usage. Skip this step only if `report_usage` was not in the `actions` array.

```ts
if (shouldReportUsage) {
  await fetch(`https://api.keelapi.com/v1/permits/${permitId}/usage`, {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${process.env.KEEL_API_KEY!}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      usage: {
        input_tokens: inputTokens,
        output_tokens: outputTokens
      }
    })
  })
}
```

Response:

```json
{
  "ok": true,
  "permit_id": "permit_01jyf4m3n8q2r6t9v1w5x7y0z",
  "usage_recorded": true
}
```

> If you skip usage reporting, the permit row retains the estimated token counts from Step 1. Budget enforcement in subsequent requests may use stale estimates until the next reconciliation cycle.

## Related pages

- [API Reference](/api-reference)
- [Executions](/executions)
- [Execute](/execute)
- [Proxy Execution](/proxy-execution)
- [Idempotency](/idempotency)
- [Errors](/errors)
